<UserControl x:Class="ClaudeCodePanel.Windows.Views.Skills.SkillsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:shared="clr-namespace:ClaudeCodePanel.Windows.Views.Shared"
             xmlns:skills="clr-namespace:ClaudeCodePanel.Windows.Views.Skills">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ═══ Row 0 — Header ═══ -->
        <Grid Grid.Row="0" Margin="48,36,48,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <shared:SectionHeader Title="Skills"
                                  Description="管理您的 Claude Code Skills"/>

            <Button x:Name="InstallBtn" Grid.Column="1"
                    Click="OnShowInstallDialog" Cursor="Hand"
                    Background="Transparent"
                    BorderBrush="{DynamicResource AccentBrush}"
                    BorderThickness="1"
                    Padding="18,9" VerticalAlignment="Center">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="20"/>
                    </Style>
                </Button.Resources>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE710;" FontFamily="Segoe MDL2 Assets"
                               FontSize="15" Foreground="{DynamicResource AccentBrush}"/>
                    <TextBlock Text="安装…" FontSize="15" FontWeight="SemiBold"
                               Foreground="{DynamicResource AccentBrush}" Margin="6,0,0,0"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- ═══ Row 1 — Tab bar + Search ═══ -->
        <Grid Grid.Row="1" Margin="48,0,48,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Segmented tab buttons -->
            <StackPanel Orientation="Horizontal" Grid.Column="0">
                <ToggleButton x:Name="InstalledTab" Click="OnSwitchTab" Tag="Installed"
                              Cursor="Hand" Padding="18,9" BorderThickness="0"
                              FontSize="15" FontWeight="SemiBold" IsChecked="True">
                    <ToggleButton.Style>
                        <Style TargetType="ToggleButton">
                            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="border" CornerRadius="20"
                                                Background="{TemplateBinding Background}"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="False">
                                                <Setter TargetName="border" Property="Background" Value="Transparent"/>
                                                <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ToggleButton.Style>
                    <TextBlock Text="我的技能"/>
                </ToggleButton>

                <ToggleButton x:Name="MarketplaceTab" Click="OnSwitchTab" Tag="Marketplace"
                              Cursor="Hand" Padding="18,9" BorderThickness="0"
                              FontSize="15" FontWeight="SemiBold" Margin="2,0,0,0">
                    <ToggleButton.Style>
                        <Style TargetType="ToggleButton">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="border" CornerRadius="20"
                                                Background="{TemplateBinding Background}"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ToggleButton.Style>
                    <TextBlock Text="浏览"/>
                </ToggleButton>
            </StackPanel>

            <shared:SearchField x:Name="SearchBar" Grid.Column="2"
                               Placeholder="搜索…" Width="220"/>
        </Grid>

        <!-- ═══ Row 2 — Content area ═══ -->
        <ScrollViewer Grid.Row="2" Margin="0,0,0,40">
            <StackPanel Margin="48,0" HorizontalAlignment="Stretch">

                <ItemsControl x:Name="InstalledList" Margin="0,0,0,32">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>

                <ItemsControl x:Name="MarketplaceGrid" Visibility="Collapsed">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="2" HorizontalAlignment="Stretch"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Margin" Value="0,0,16,16"/>
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                </ItemsControl>

                <ProgressBar x:Name="LoadingSpinner" Width="24" Height="24"
                            IsIndeterminate="True" Visibility="Collapsed"
                            HorizontalAlignment="Center" Margin="0,40,0,0"/>

                <shared:EmptyState x:Name="InstalledEmpty" IconGlyph="&#xE94A;"
                                  Message="暂无已安装的 Skill" Visibility="Collapsed"/>
                <shared:EmptyState x:Name="MarketplaceEmpty" IconGlyph="&#xE94A;"
                                  Message="未找到相关 Skill" Visibility="Collapsed"/>

            </StackPanel>
        </ScrollViewer>

        <!-- ═══ Install Dialog Overlay ═══ -->
        <skills:SkillInstallDialog x:Name="InstallDialog" Grid.RowSpan="3"
                                   Visibility="Collapsed"
                                   InstallClicked="OnInstallSkill"
                                   CancelClicked="OnCancelInstall"/>
    </Grid>
</UserControl>
